const INVALID_CREDENTIALS = 'Invalid credentials'
const USER_EXISTS = 'User already exists'
const USER_NOT_FOUND = 'User not found'
const MISSING_ID_OR_PASSWORD = 'id and password required'
const MISSING_REFRESH_TOKEN = 'refresh token required'
const INVALID_REFRESH_TOKEN = 'Invalid or expired refresh token'

export {
    INVALID_CREDENTIALS,
    USER_EXISTS,
    MISSING_ID_OR_PASSWORD,
    USER_NOT_FOUND,
    MISSING_REFRESH_TOKEN,
    INVALID_REFRESH_TOKEN,
}
